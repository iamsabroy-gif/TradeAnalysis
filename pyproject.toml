[tool.vercel]
# Vercel deploys this repository as a native FastAPI backend. Point it at the
# FastAPI `app` instance so it doesn't have to guess between several candidates.
entrypoint = "backend.app.api.main:app"

[tool.pytest.ini_options]
pythonpath = ["."]



